# Core dependencies for WSJT-X/JS8-Call Auto Grid
# Minimal set of packages for cross-platform compatibility

# Serial communication for GPS devices
pyserial>=3.5

# Configuration file parsing
configparser>=5.0.0

# GPSD support is now built-in - no external module required

# Optional: For creating Windows executables
# pyinstaller>=4.0

psutil 